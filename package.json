{
  "name": "discord-typescript-bot",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "tsnd --prefer-ts true --no-notify -r tsconfig-paths/register --watch src --respawn --transpile-only src/bot.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "dotenv": "^16.0.3",
    "ts-node-dev": "^2.0.0",
    "tsconfig-paths": "^4.1.2",
    "typescript": "^4.9.4"
  },
  "dependencies": {
    "discord.js": "^14.7.1"
  }
}
